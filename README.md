# TinyApp Project

TinyApp is a full stack web application built with Node and Express that allows users to shorten long URLs (à la bit.ly).

## Dependencies

- Node.js
- Express
- EJS
- bcryptjs
- cookie-session

## Getting Started

- Install all dependencies (using the `npm install` command).
- Run the development web server using the `node express_server.js` command.
- The default listening port is 8080. TinyApp functions can be accessed from your web browser at http://localhost:8080/register when the server is running.

## Final Product

!["Registration page"](Images/register.png)
!["Login page"](Images/login.png)
!["Main screen displaying URLs"](Images/urls.png)
!["Tiny link creation page"](Images/create.png)
!["Edit page"](Images/edit.png)
